import React from 'react';
import styles from "./LoadingService.module.scss"
const LoadingService = () => {
    return (
        <div className={styles.loading__service} />
    );
};

export default LoadingService;